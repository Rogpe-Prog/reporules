{
  "name": "meu-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream",
    "build": "lerna run build --stream",
    "test": "lerna run test --stream",
    "dev": "concurrently \"lerna run dev --stream\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "lerna": "^8.1.8"
  }
}
